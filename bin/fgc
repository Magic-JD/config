#!/bin/bash

function fgc() {
    if ! [[ $(git rev-parse --is-inside-work-tree) == 'true' ]]; then
        echo "Not a git directory!";
        exit 1;
    fi;

    VAR=$(git branch | fzf)

    if [[ -n $VAL ]]; then
        git checkout $VAL
    fi
}
