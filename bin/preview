#!/bin/bash

if [[ -d "$1" ]]; then
    eza -l --color=always -a --icons --git "$1"
    exit 0
fi

if [[ -f "$1" ]]; then
    bat --color=always --style=header,grid --line-range :300 "$1"
    exit 0
fi

echo "$1"
