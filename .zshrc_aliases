#Alias for ls to make it look better
alias ls='eza -a --icons'
alias ll='eza -l -a --icons --git'
alias tree='eza --tree --icons=always --color=always | less -R'

# Use zoxide to change directory
alias cd='cdls'

# Neovim
alias vi='vii'
alias vim='vim -u NONE'

# Starship
alias landing='export STARSHIP_CONFIG=~/.starship-mini.toml'
alias takeoff='export STARSHIP_CONFIG=~/.starship.toml'

# Better top
alias top='btop'

# Bat coloring
alias tail='battail'

# Git
alias fgc='git checkout $(_fzf_git_branches)'

# Fuck
alias f='fuck'

# Is fast
alias is='is-fast'
